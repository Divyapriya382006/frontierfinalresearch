fastapi==0.115.0
uvicorn[standard]==0.30.6
pydantic==2.9.2
requests==2.32.3
pytest==8.3.3
pytest-mock==3.14.0

# --- LLM provider: install ONLY the one(s) you plan to use via LLM_PROVIDER ---
anthropic==0.34.2      # LLM_PROVIDER=anthropic
# google-genai          # LLM_PROVIDER=gemini  (pip install google-genai)
# (groq needs no extra package - called over HTTP via `requests`, OpenAI-compatible endpoint)
# (ollama needs no extra package - it's called over HTTP via `requests`,
#  but you must have the Ollama app/server running locally: https://ollama.com)
